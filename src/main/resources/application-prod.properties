spring.application.name=gomwifiv1

# Server configuration
server.port=${PORT}

# Database Configuration
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pool Configuration
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=12
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Session Configuration
server.servlet.session.timeout=30m
spring.session.timeout=1800

# UniFi Configuration
unifi.controller-url=${UNIFI_CONTROLLER_URL:https://169.254.145.11}
unifi.username=${UNIFI_USERNAME:icnhaiti1@gmail.com}
unifi.password=${UNIFI_PASSWORD:Pjrappach251184}
unifi.site=${UNIFI_SITE:default}
